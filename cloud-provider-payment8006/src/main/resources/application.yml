server:
  port: 8006

spring:
  application:
    name: Consul-payment-provider

    #Consul注册地址
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name} #对外暴露的服务名称

